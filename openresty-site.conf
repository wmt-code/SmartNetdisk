# SmartNetdisk 域名配置
# 将此文件放置到 OpenResty 的 conf.d 目录
# 1Panel 默认路径: /opt/1panel/apps/openresty/openresty/conf.d/

server {
    listen 80;
    server_name netdisk.abcsummer.site;  # 修改为您的域名

    # HTTP 重定向到 HTTPS（如果配置了 SSL）
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:9080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持（如需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 大文件上传配置
    client_max_body_size 10G;
    proxy_connect_timeout 600;
    proxy_send_timeout 600;
    proxy_read_timeout 600;
}

# HTTPS 配置（配置 SSL 证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name netdisk.abcsummer.site;  # 修改为您的域名
#
#     # SSL 证书路径（1Panel 自动申请的证书路径）
#     ssl_certificate /opt/1panel/apps/openresty/openresty/www/sites/netdisk.abcsummer.site/ssl/fullchain.pem;
#     ssl_certificate_key /opt/1panel/apps/openresty/openresty/www/sites/netdisk.abcsummer.site/ssl/privkey.pem;
#
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:50m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers off;
#
#     location / {
#         proxy_pass http://127.0.0.1:9080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
#
#     client_max_body_size 10G;
#     proxy_connect_timeout 600;
#     proxy_send_timeout 600;
#     proxy_read_timeout 600;
# }
